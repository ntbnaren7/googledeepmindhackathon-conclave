/**
 * Global type definitions for Conclave
 */

export interface Speaker {
  id: string;
  label: string;
  isHuman: boolean;
}

// TODO: Define all shared types (AudioChunk, TranscriptSegment, etc.)
