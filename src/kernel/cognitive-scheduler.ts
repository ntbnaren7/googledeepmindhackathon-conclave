export class CognitiveScheduler {
  constructor(private registry: any) {}
  async dispatch(snapshot: any, delta: any, blackboard: any) {
    // TODO: Promise.allSettled over agents
    return [];
  }
}
