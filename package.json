{
  "name": "conclave",
  "version": "0.1.0",
  "description": "A Cognitive Operating System for real-time technical decision making",
  "main": "src/index.ts",
  "scripts": {
    "dev": "ts-node src/index.ts",
    "dev:ui": "vite",
    "build": "tsc",
    "build:ui": "vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@google/generative-ai": "latest",
    "ws": "^8.x"
  },
  "devDependencies": {
    "typescript": "^5.x",
    "ts-node": "^10.x",
    "vitest": "^2.x",
    "@types/ws": "^8.x",
    "eslint": "^9.x",
    "@typescript-eslint/eslint-plugin": "latest",
    "@typescript-eslint/parser": "latest",
    "prettier": "^3.x",
    "vite": "^5.x",
    "vite-tsconfig-paths": "^5.x"
  }
}
